# Dictionaries: built-in Dict k v type
# Keys require Eq, all operations return new dicts (immutable)

main () = {
  # Create from list of tuples
  let ages = Dict.from_list [("alice", 30), ("bob", 25), ("charlie", 35)]
  print $"ages: {ages}"
  print $"size: {(Dict.size ages)}"

  # Lookup
  let alice_age = Dict.get "alice" ages
  print $"alice: {alice_age}"

  let unknown = Dict.get "dave" ages
  print $"dave: {unknown}"

  # Insert and update
  let ages2 = Dict.put "dave" 28 ages
  print $"added dave: {ages2}"

  let ages3 = Dict.put "alice" 31 ages2
  print $"updated alice: {ages3}"

  # Remove
  let ages4 = Dict.remove "bob" ages3
  print $"removed bob: {ages4}"

  # Membership
  print $"has alice: {(Dict.member "alice" ages)}" 
  print $"has dave: {(Dict.member "dave" ages)}"

  # Keys and values
  print $"keys: {(Dict.keys ages)}"
  print $"values: {(Dict.values ages)}"

  # Round-trip through list
  let pairs = Dict.to_list ages
  print $"to_list: {pairs}"

  # Start from empty
  let d = 
    Dict.empty
    |> Dict.put "y" 2
    |> Dict.put "x" 1
  print $"from empty: {d}"
}
