import Std.List (length)

# foo size = size + 1
# main () = println (debug (foo 3))

length_status length = case length {
  0 -> "empty"
  _ -> "not empty"
}

main () = length_status (List.length []) |> println
