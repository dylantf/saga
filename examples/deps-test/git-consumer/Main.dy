module Main

import MathLib (add, square, tripled)

pub fun main : Unit -> Unit
main () = {
  let result = add 10 20
  println (show result)

  let sq = square 7
  println (show sq)

  let tri = tripled 4
  println (show tri)
}
