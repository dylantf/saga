# Tuples -- any arity, no predefinition needed

fun distance : (p: (Float, Float)) -> Float
distance (x, y) = x * x + y * y

fun swap : (p: (a, b)) -> (b, a)
swap (x, y) = (y, x)

fun fst : (p: (a, b)) -> a
fst (x, _) = x

fun snd : (p: (a, b)) -> b
snd (_, y) = y

main () = {
  # Pair
  let point = (3.0, 4.0)
  dbg ("point: " <> debug point)
  dbg ("distance squared: " <> show (distance point))

  # Swap
  let flipped = swap point
  dbg ("swapped: " <> debug flipped)

  # Triple
  let rgb = (255, 128, 0)
  dbg ("color: " <> debug rgb)

  # Accessors
  let pair = ("hello", 42)
  dbg ("fst: " <> fst pair)
  dbg ("snd: " <> show (snd pair))

  # 5-tuple (no size limit)
  dbg (1, 2, 3, 4, 5)

  # Nested
  let nested = ((1, 2), (3, 4))
  dbg ("nested: " <> debug nested)

  # Pattern matching in case
  case nested {
    ((a, b), (c, d)) -> dbg ("sum: " <> show (a + b + c + d))
  }
}
