import Std.Env (system_env)
import Std.IO (console, println)

main () = {
  let home = get! "HOME"
  case home {
    Just h -> println h
    Nothing -> println "Couldn't find $HOME"
  }
} with {console, system_env}
