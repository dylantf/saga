type Foo = Foo String

impl Show for Foo {
  show f = case f {
    Foo s -> s
  }
}

type Bar = Bar String

impl Show for Bar {
  show (Bar s) = s
}

main () = {
  let foo = Foo "foo string"
  let bar = Bar "bar string"

  println $"Foo: {foo}"
  println $"Bar: {bar}"
} with console
