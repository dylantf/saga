import Std.Actor (Process, Timer, beam_actor)

fun spawn_in_effect : Unit -> Unit needs {Process}
spawn_in_effect () = {
  let _ = spawn! (fun () -> dbg "ran")
  ()
}

main () = {
  spawn_in_effect ()
  sleep! 200
} with beam_actor
