module Main

import Std.IO.Unsafe (print_stdout)

import Lib (Boxed, Size)

fun apply_box : (Boxed Int -> Int) -> Int
apply_box f = f (Boxed 41)

fun main : Unit -> Unit
main () =
  let f = size
  if apply_box f == 42 then print_stdout "ok\n"
  else print_stdout "bad\n"
