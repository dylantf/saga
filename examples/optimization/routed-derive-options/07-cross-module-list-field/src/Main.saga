module Main

import Lib (Options, Encodable)

record User {
  scores: List Int,
} deriving (Encodable)

main () = {
  let h = Lib.options 10
  let result = Lib.serialize (User { scores: [1, 2] }) with h
  dbg (show result)
}

