module Main

import Lib (Options, Encodable)

record User {
  first: Int,
  details: Maybe Int,
} deriving (Encodable)

main () = {
  let h = Lib.options 10
  let result = Lib.serialize (User { first: 1, details: Just 2 }) with h
  dbg (show result)
}
