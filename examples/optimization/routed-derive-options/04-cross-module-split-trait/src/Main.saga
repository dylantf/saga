module Main

import Lib (Options, Encodable)

handler options_10 for Options {
  get_options () = resume 10
}

record User {
  first: Int,
  second: Int,
} deriving (Encodable)

main () = {
  let result = Lib.serialize (User { first: 1, second: 2 }) with options_10
  dbg (show result)
}
