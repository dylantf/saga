# val constants

val pi = 3.14159

val app_name = "saga"

val numbers = [1, 2, 3]

val computed = List.map (fun x -> x * 2) [1, 2, 3]

main () = {
  dbg (show pi)
  dbg app_name
  dbg (debug numbers)
  dbg (debug computed)
}
