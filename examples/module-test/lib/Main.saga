module Main

import MathLib (Describe)

pub record Person {
  name: String
}

impl Describe for Person {
  describe_it p = $"Name is: {p.name}"
}

main () = {
  let dylan = Person { name: "Dylan" }
  dylan |> describe_it |> println
} with {console}
