import Std.Bool (not)
import Std.Maybe (Maybe)
import Std.Result (Result)
import Std.List
import Std.Dict
import Std.Int
import Std.Float
import Std.String
import Std.Tuple (fst, snd)

trait Show a {
  fun show (x: a) -> String
}

fun print (x: a) -> () where {a: Show}
