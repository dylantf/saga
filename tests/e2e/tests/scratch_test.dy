import Std.Test (describe, test, assert_eq)

describe "repro" {
  test "case on Dynamic.run Err result" {

    case Ok 123 {
      Ok _ -> assert_eq 1 1
      Err _ -> assert_eq 1 1
    }
  }
}

# import Std.Dynamic (Dynamic, Decode, from_erlang, string, int)

# let data =
#   from_erlang (Dict.from_list [
#     ("name", from_erlang 42),
#     ("age", from_erlang "wrong"),
#   ])

# let result = Dynamic.run data (fun () -> {
#   let name = Decode.decode_field! "name" string
#   let age = Decode.decode_field! "age" int
#   (name, age)
# })
